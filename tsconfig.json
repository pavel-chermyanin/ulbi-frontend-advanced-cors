{
  "compilerOptions": {
    "outDir": "./dist/",
    "noImplicitAny": true, // Эта настройка указывает, что TypeScript не должен разрешать тип any без явного указания. Если переменная не имеет указанного типа, это вызовет ошибку.
    "module": "ESNext", // Эта настройка определяет формат модулей, который будет использоваться при компиляции TypeScript. В данном случае, используется "ESNext", что означает использование синтаксиса ES6/ES2015 и выше для модулей.
    "target": "es5", // Эта настройка указывает на целевую версию JavaScript, в которую будут компилироваться TypeScript файлы. В данном случае, цель - "es5", что означает, что код будет совместим с ECMAScript 5.
    "jsx": "react-jsx", // Эта настройка указывает, что TypeScript должен поддерживать синтаксис JSX для работы с React компонентами.
    "allowJs": true, //  Эта настройка позволяет использовать файлы JavaScript в вашем проекте, а не только TypeScript. Если установлена в true, то TypeScript будет компилировать и JavaScript файлы.
    "moduleResolution": "node", // Эта настройка определяет алгоритм разрешения модулей, который будет использоваться. В данном случае, используется "node", что означает использование алгоритма разрешения модулей, аналогичного Node.js.
    "allowSyntheticDefaultImports": true, // : Эта настройка разрешает импорты синтетических (генерируемых) модулей с использованием синтаксиса import x from 'y', где 'y' - это модуль, который не предоставляет явный экспорт default.
    "esModuleInterop": true,
    "baseUrl": ".",
    "paths": {
      "*": [
        "./src/*"
      ] // все пути будут идти относительно папки src
    },
  },
  // В разделе "ts-node" настроены параметры для ts-node, инструмента, позволяющего выполнять TypeScript файлы непосредственно в Node.js. Здесь "module": "CommonJS" указывает формат модулей для исполнения TypeScript файлов в Node.js, который будет использоваться при использовании ts-node.
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  },
  "include": [
    "./config/jest/setupTests.ts",
    "./src/**/*.ts",
    "./src/**/*.tsx"
  ]
}